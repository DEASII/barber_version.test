package ku.cs.xylaz.controller;

import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class HomeController {

//    @GetMapping("/") // เส้นทางหลัก
//    public ResponseEntity<Void> redirectToReactApp() {
//        return ResponseEntity.status(302) // สถานะการเปลี่ยนเส้นทาง
//                .header("Location", "http://localhost:3000/") // เปลี่ยนเส้นทางไปที่ React app
//                .build();
//    }
}